name: Deploy Branch Preview to GitHub Pages

on:
  push:
    branches-ignore:
      - gh-pages

permissions:
  contents: write

concurrency:
  group: branch-pages-${{ github.ref_name }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Publish current branch to gh-pages subpath
        env:
          BRANCH_PATH: ${{ github.ref_name }}
          REPO_URL: https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
          BOT_NAME: github-actions[bot]
          BOT_EMAIL: 41898282+github-actions[bot]@users.noreply.github.com
        run: |
          set -euo pipefail

          WORKDIR="$(mktemp -d)"
          trap 'rm -rf "$WORKDIR"' EXIT

          if git ls-remote --exit-code --heads "$REPO_URL" gh-pages >/dev/null 2>&1; then
            git clone --depth 1 --branch gh-pages "$REPO_URL" "$WORKDIR"
          else
            git init "$WORKDIR"
            cd "$WORKDIR"
            git checkout -b gh-pages
            git remote add origin "$REPO_URL"
          fi

          TARGET_PATH="$WORKDIR/$BRANCH_PATH"
          if [ "$BRANCH_PATH" = "main" ]; then
            TARGET_PATH="$WORKDIR"
          else
            mkdir -p "$TARGET_PATH"
          fi

          rsync -av --delete \
            --exclude '.git/' \
            --exclude '.github/' \
            --exclude '.idea/' \
            --exclude 'test-results/' \
            --exclude 'generated-assets/' \
            --exclude '.DS_Store' \
            ./ "$TARGET_PATH/"

          touch "$WORKDIR/.nojekyll"

          cd "$WORKDIR"
          git config user.name "$BOT_NAME"
          git config user.email "$BOT_EMAIL"
          git add -A

          if git diff --cached --quiet; then
            echo "No Pages changes to publish."
            exit 0
          fi

          git commit -m "Deploy ${GITHUB_SHA} to ${BRANCH_PATH}"
          git push "$REPO_URL" gh-pages
